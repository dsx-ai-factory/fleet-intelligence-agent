✓ Fleet Intelligence Agent deployed successfully!

NAMESPACE: {{ .Release.Namespace }}
{{- if .Values.enroll.enabled }}
ENROLLMENT: Enabled
Open https://fleet-intelligence.ngc.nvidia.com to view your Fleet Intelligence status.
{{- else }}
ENROLLMENT: Disabled (standalone mode)
{{- end }}

Check status:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "fleet-intelligence-agent.name" . }}
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "fleet-intelligence-agent.name" . }} --tail=50

Docs: https://github.com/dsx-ai-factory/fleet-intelligence-agent
